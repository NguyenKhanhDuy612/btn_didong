import 'package:flutter/material.dart';

class GopY extends StatelessWidget {
  const GopY({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
